{
  "name": "image-processor",
  "version": "1.0.0",
  "description": "AWS Lambda image processing function (resize + thumbnail + AVIF conversion)",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "test": "echo \"No tests\" && exit 0"
  },
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.901.0",
    "sharp": "^0.34.3"
  }
}
